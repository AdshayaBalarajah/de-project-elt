version: 2

models:
  - name: stg_weather
    columns:
      - name: observed_at
        tests:
          - not_null
          - unique
      - name: temperature_c
        tests:
          - not_null
